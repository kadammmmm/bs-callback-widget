{
  "name": "bs-callback-backend",
  "version": "1.0.0",
  "description": "Backend service for Abandoned Call Callback Widget",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "cd .. && npm install && npm run build && cd backend"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
